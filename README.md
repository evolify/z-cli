# web-cli
A cli to create web app.

* ### install

  `npm i -g @evolify/web-cli`

* ### create react app:

  ```shell
  web react <app-name>
  cd <app-name>
  yarn dev	# default run at 8080 port
  ```

